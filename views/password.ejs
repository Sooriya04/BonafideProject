<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Forgot Password</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        background-color: #fffdf5;
        font-family: 'Arial', cursive;
      }
      .form-card {
        background: white;
        border-radius: 20px;
        max-width: 380px;
        width: 100%;
        padding: 2.5rem 2rem;
      }
      .title {
        color: #800000;
        font-size: 1.8rem;
        font-weight: bold;
      }
      .subtitle {
        color: #a05252;
        font-size: 1rem;
      }
      .form-control {
        border-radius: 10px;
        font-size: 1rem;
      }
      .form-control:focus {
        border-color: #800000;
      }
      .btn-login {
        background: #800000;
        color: white;
        border-radius: 20px;
        padding: 10px;
        font-size: 1.1rem;
      }
      .btn-login:hover {
        background: #660000;
        color: #fff !important;
      }
    </style>
  </head>
  <body>
    <div
      class="container d-flex justify-content-center align-items-center min-vh-100"
    >
      <div class="card shadow-lg px-4 py-5 form-card">
        <h2 class="text-center title mb-2">Forgot Password</h2>
        <p class="subtitle text-center mb-4">
          Enter your email to reset password.
        </p>

        <% if (message) { %>
        <div class="alert alert-info"><%= message %></div>
        <% } %> <% if (error) { %>
        <div class="alert alert-danger"><%= error %></div>
        <% } %>

        <form action="/auth/forgot-password" method="POST">
          <div class="mb-3">
            <label for="email" class="form-label">Email ID</label>
            <input
              type="email"
              class="form-control"
              id="email"
              name="email"
              placeholder="Enter your registered email"
              required
            />
          </div>
          <button type="submit" class="btn btn-login w-100 mb-3">
            Send Reset Link
          </button>
        </form>

        <div class="text-center mt-2">
          <a
            href="/auth/login"
            style="color: #800000; text-decoration: underline"
            >Back to Login</a
          >
        </div>
      </div>
    </div>
  </body>
</html>
