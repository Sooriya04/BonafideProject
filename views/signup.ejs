<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Sign Up</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,100..900;1,100..900&display=swap"
      rel="stylesheet"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/css/signup.css" />

    <script type="module">
      import {
        validateSignupForm,
        togglePasswordVisibility,
      } from '/js/validation.js';
      window.validateForm = validateSignupForm;
      window.togglePasswordVisibility = togglePasswordVisibility;
    </script>
  </head>
  <body>
    <header class="app-header d-flex align-items-center px-3 py-2 shadow-sm">
      <img
        src="https://www.tce.edu/themes/gavias_edmix/logo.svg"
        alt="TCE Logo"
      />
    </header>

    <div class="container">
      <div class="card form-card">
        <!-- Alert Placeholder -->
        <div id="alertPlaceholder">
          <% if (typeof message !== 'undefined' && message) { %>
          <div
            class="alert alert-success alert-dismissible fade show"
            role="alert"
          >
            <strong>Success!</strong> <%= message %>
            <button
              type="button"
              class="btn-close"
              data-bs-dismiss="alert"
              aria-label="Close"
            ></button>
          </div>
          <% } %>
        </div>

        <h2 class="text-center title mb-2">Sign Up</h2>
        <p class="subtitle text-center">Create a new account.</p>

        <form
          name="signupForm"
          action="/auth/signup"
          method="POST"
          onsubmit="return validateForm()"
        >
          <div class="mb-3">
            <label for="name" class="form-label">Name</label>
            <input
              type="text"
              name="name"
              class="form-control"
              id="name"
              placeholder="Enter your name"
            />
          </div>
          <div class="mb-3">
            <label for="email" class="form-label">Email ID</label>
            <input
              type="email"
              name="email"
              class="form-control"
              id="email"
              placeholder="Enter your email"
            />
          </div>
          <div class="mb-3 password-wrapper">
            <label for="password" class="form-label">Password</label>
            <input
              type="password"
              name="password"
              class="form-control"
              id="password"
              placeholder="Enter password"
            />
            <i
              id="togglePassword"
              class="bi bi-eye toggle-password"
              onclick="togglePasswordVisibility()"
              title="Show/Hide Password"
            ></i>
          </div>
          <button type="submit" class="btn btn-login w-100">Sign Up</button>
        </form>

        <div class="text-center mt-3">
          <span>Already have an account?</span>
          <a href="/auth/login" class="text-decoration-none text-switch"
            >Login</a
          >
        </div>

        <button id="googleLoginBtn" type="button" class="googleLoginBtn">
          <i class="bi bi-google google-icon"></i> Continue with Google
        </button>
      </div>
    </div>

    <!-- Pass Firebase Config to JS -->
    <script>
      window.firebaseConfig = {
        apiKey: '<%= process.env.apiKey %>',
        authDomain: '<%= process.env.authDomain %>',
        projectId: '<%= process.env.projectId %>',
        storageBucket: '<%= process.env.storageBucket %>',
        messagingSenderId: '<%= process.env.messagingSenderId %>',
        appId: '<%= process.env.appID %>',
      };
    </script>

    <script type="module" src="/js/googleAuth.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
