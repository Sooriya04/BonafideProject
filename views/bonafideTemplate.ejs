<!DOCTYPE html>
<html>
  <head>
    <title>Bonafide Certificate</title>
    <style>
      @page {
        size: A4;
        margin: 20mm;
      }
      body {
        font-family: 'Times New Roman', serif;
        margin: 0;
        padding: 0;
        background: white;
      }
      .certificate {
        width: 95%;
        margin: auto;
        line-height: 1.5;
        font-size: 14px;
        box-sizing: border-box;
      }
      .center {
        text-align: center;
      }
      .row {
        display: flex;
        justify-content: space-between;
        margin: 10px 0 5px 0;
        font-size: 13px;
      }
      .header-line {
        border-top: 2px solid black;
        margin: 5px 0 10px 0;
      }
      h3.center {
        font-size: 16px;
        margin: 0;
      }
      .body-text {
        text-align: justify;
        font-size: 14px;
        margin-top: 10px;
      }
      .signature {
        margin-top: 120px;
        text-align: right;
        font-weight: bold;
        font-size: 16px;
      }
      .footer-line {
        border-top: 1px solid black;
        margin-top: 30px;
      }
      .footer {
        text-align: center;
        font-size: 12px;
        margin-top: 5px;
      }
    </style>
  </head>
  <body>
    <div class="certificate">
      <h3 class="center">
        THIAGARAJAR COLLEGE OF ENGINEERING, MADURAI - 625015
      </h3>
      <p class="center">
        (A Govt.Aided Autonomous Institution affiliated to Anna University)
      </p>

      <div class="header-line"></div>

      <div class="row">
        <p>No.D/2025</p>
        <p>Date: <%= String(formData.date) %></p>
      </div>

      <h3 class="center">BONAFIDE CERTIFICATE</h3>

      <p class="body-text">
        Certified that
        <strong
          ><%= String(formData.title) %> <%= String(formData.name) %></strong
        >, (Roll No: <strong><%= String(formData.rollno) %></strong>)
        <strong
          ><%= String(formData.relation) %> <%= String(formData.parentName)
          %></strong
        >
        is a bonafide student of this College studying in
        <strong
          ><%= String(formData.year) %> Year, <%= String(formData.course) %> -
          <%= String(formData.branch) %></strong
        >
        during the year <strong><%= formData.academicYear %></strong>.
      </p>

      <p class="body-text">
        This Certificate is issued to enable him/her to apply for
        <strong><%= String(formData.certificateFor) %></strong>
        <% if (formData.scholarshipType) { %> (<%=
        String(formData.scholarshipType) %>) <% } %>
      </p>

      <p class="signature">PRINCIPAL</p>

      <div class="footer-line"></div>
      <p class="footer">
        Ph.No. 0452-2482240-42 &nbsp;&nbsp;&nbsp; Fax No: 0452-2483427
        &nbsp;&nbsp;&nbsp; Email: tceoffice@tce.edu
      </p>
    </div>
  </body>
</html>
