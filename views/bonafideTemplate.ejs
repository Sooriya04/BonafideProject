<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Bonafide Certificate</title>
    <style>
      body {
        font-family: 'Times New Roman', Times, serif;
        margin: 0;
        padding: 0;
        background: white;
        color: #000;
        width: 100%;
        height: 100%;
        line-height: 1.6;
      }

      .certificate-container {
        width: 800px;
        min-height: 1120px;
        margin: 0 auto;
        padding: 40px 50px;
        box-sizing: border-box;
        position: relative;
      }

      .letterhead {
        text-align: center;
        margin-bottom: 20px;
      }

      .institution-name {
        font-size: 22px;
        font-weight: bold;
        margin: 0;
        line-height: 1.3;
      }

      .institution-details {
        font-size: 16px;
        margin: 5px 0 15px 0;
      }

      .header-line {
        border-top: 2px solid black;
        margin: 10px 0 30px 0;
      }

      .document-info {
        display: flex;
        justify-content: space-between;
        font-size: 16px;
        margin-bottom: 30px;
      }

      .document-title {
        text-align: center;
        font-size: 22px;
        font-weight: bold;
        margin: 20px 0 40px 0;
        text-decoration: underline;
      }

      .body-text {
        font-size: 18px;
        text-align: justify;
        line-height: 1.8;
        margin-bottom: 20px;
      }

      .signature {
        margin-top: 100px;
        text-align: right;
        font-weight: bold;
        font-size: 18px;
      }

      .footer-line {
        border-top: 1px solid black;
        margin-top: 80px;
      }

      .footer {
        text-align: center;
        font-size: 16px;
        margin-top: 10px;
      }
    </style>
  </head>
  <body>
    <div class="certificate-container">
      <div class="letterhead">
        <h1 class="institution-name">
          THIAGARAJAR COLLEGE OF ENGINEERING, MADURAI - 625015
        </h1>
        <p class="institution-details">
          (A Govt. Aided Autonomous Institution affiliated to Anna University)
        </p>
      </div>

      <div class="header-line"></div>

      <div class="document-info">
        <span>No.D/<%= formData.documentNumber || '2025' %></span>
        <span>Date: <%= formData.date %></span>
      </div>

      <h2 class="document-title">BONAFIDE CERTIFICATE</h2>

      <p class="body-text">
        Certified that
        <strong><%= formData.title %> <%= formData.name %></strong>, (Roll No:
        <strong><%= formData.rollno %></strong>)
        <strong><%= formData.relation %> <%= formData.parentName %></strong>
        is a bonafide student of this College studying in
        <strong
          ><%= formData.year %> Year, <%= formData.course %> - <%=
          formData.branch %></strong
        >
        during the year
        <strong><%= formData.academicYear || '2025-2026' %></strong>.
      </p>

      <p class="body-text">
        This Certificate is issued to enable <%= formData.title === 'Mr.' ?
        'him' : 'her' %> to apply for
        <strong><%= formData.certificateFor %></strong>
        <% if (formData.scholarshipType) { %> (<%= formData.scholarshipType %>)
        <% } %>.
      </p>

      <div class="signature">PRINCIPAL</div>

      <div class="footer-line"></div>

      <div class="footer">
        Ph.No. 0452-2482240-42 &nbsp;&nbsp;&nbsp; Fax No: 0452-2483427
        &nbsp;&nbsp;&nbsp; Email: tceoffice@tce.edu
      </div>
    </div>
  </body>
</html>
