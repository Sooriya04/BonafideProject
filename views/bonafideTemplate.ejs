<!DOCTYPE html>
<html>
  <head>
    <title>Bonafide Certificate</title>
    <style>
      @page {
        size: A4;
        margin: 30mm;
      }
      body {
        font-family: Arial, serif;
        margin: 0;
        padding: 0;
        background: white;
      }
      .certificate {
        width: 100%;
        margin: auto;
        line-height: 2;
        font-size: 23px;
        box-sizing: border-box;
      }
      .center {
        text-align: center;
      }
      .row {
        display: flex;
        justify-content: space-between;
        margin: 12px 0;
        font-size: 22px; /* bigger for PDF */
      }
      p.center {
        margin-top: 2px;
        font-size: 22px;
      }
      .header-line {
        border-top: 2px solid black;
        margin: 12px 0 22px;
      }
      h3.center:first-of-type {
        white-space: nowrap; /* prevents line breaks */
        font-size: 26px; /* adjust to fit A4 */
        margin: 0 auto 10px auto;
      }

      .body-text {
        text-align: justify;
        font-size: 25px; /* increase for PDF readability */
        margin-top: 28px;
        padding: 1rem;
      }
      .signature {
        margin-top: 320px;
        text-align: right;
        font-weight: bold;
        font-size: 28px;
        margin-bottom: -10px;
      }
      .footer-line {
        border-top: 1px solid black;
        margin-top: 60px;
      }
      .footer {
        text-align: center;
        font-size: 22px; /* readable in PDF */
        margin-top: 5px;
      }
    </style>
  </head>
  <body>
    <div class="certificate">
      <h3 class="center">
        THIAGARAJAR COLLEGE OF ENGINEERING, MADURAI - 625015
      </h3>
      <p class="center">
        (A Govt.Aided Autonomous Institution affiliated to Anna University)
      </p>

      <div class="header-line"></div>

      <div class="row">
        <p>No.D/2025</p>
        <p>Date: <%= String(formData.date) %></p>
      </div>

      <h3 class="center">BONAFIDE CERTIFICATE</h3>

      <p class="body-text">
        &nbsp; &nbsp; Certified that
        <strong
          ><%= String(formData.title) %> <%= String(formData.name) %></strong
        >, (Roll No: <strong><%= String(formData.rollno) %></strong>)
        <strong
          ><%= String(formData.relation) %> <%= String(formData.parentName)
          %></strong
        >
        is a bonafide student of this College studying in
        <strong
          ><%= String(formData.year) %> Year, <%= String(formData.course) %> -
          <%= String(formData.branch) %></strong
        >
        during the year <strong><%= formData.academicYear %></strong>.
      </p>

      <p class="body-text">
        &nbsp; This Certificate is issued to enable him/her to apply for
        <strong><%= String(formData.certificateFor) %></strong>
        <% if (formData.scholarshipType) { %> (<%=
        String(formData.scholarshipType) %>) <% } %>.
      </p>

      <p class="signature">PRINCIPAL</p>

      <div class="footer-line"></div>

      <p class="footer">
        Ph.No. 0452-2482240-42 &nbsp;&nbsp;&nbsp; Fax No: 0452-2483427
        &nbsp;&nbsp;&nbsp; Email: tceoffice@tce.edu
      </p>
    </div>
  </body>
</html>
