<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Bonafide Certificate</title>
    <style>
      body {
        font-family: 'Times New Roman', Times, serif;
        margin: 0;
        padding: 0;
        background: white;
        color: #000;
        width: 100%;
        height: 100%;
        line-height: 1.6;
      }

      .certificate-container {
        width: 800px; /* about A4 width */
        min-height: 1120px; /* about A4 height */
        margin: 0 auto;
        padding: 40px 50px;
        box-sizing: border-box;
        position: relative;
      }

      .letterhead {
        text-align: center;
        margin-bottom: 20px;
      }

      .institution-name {
        font-size: 18px; /* was 22pt */
        font-weight: 800;
        margin: 0;
        line-height: 1.3;
      }

      .institution-details {
        font-size: 13px; /* was 13pt */
        margin: 5px 0 15px 0;
      }

      .header-line {
        border-top: 2px solid black;
        margin: 10px 0 30px 0;
      }

      .document-info {
        display: flex;
        justify-content: space-between;
        font-size: 13px;
        margin-bottom: 30px;
      }

      .document-title {
        text-align: center;
        font-size: 19px;
        font-weight: bold;
        margin: 20px 0 40px 0;
      }

      .body-text {
        font-size: 16px; /* was 18pt */
        text-align: justify;
        line-height: 1.7;
        margin-bottom: 20px;
      }

      .signature {
        margin-top: 120px;
        text-align: right;
        font-weight: bold;
        font-size: 16px;
        font-weight: 700;
      }

      .footer-line {
        border-top: 1px solid black;
        margin-top: 60px;
      }

      .footer {
        text-align: center;
        font-size: 13px;
        margin-top: 5px;
      }
    </style>
  </head>
  <body>
    <div class="certificate-container">
      <div class="letterhead">
        <h1 class="institution-name">
          THIAGARAJAR COLLEGE OF ENGINEERING, MADURAI - 625015
        </h1>
        <p class="institution-details">
          (A Govt.Aided Autonomous Institution affiliated to Anna University)
        </p>
      </div>

      <div class="header-line"></div>

      <div class="document-info">
        <span>No.D/2025</span>
        <span>Date: <%= formData.date %></span>
      </div>

      <h2 class="document-title">BONAFIDE CERTIFICATE</h2>

      <p class="body-text">
        Certified that
        <strong
          ><%- String(formData.title) %> <%- String(formData.name) %></strong
        >, (Roll No: <strong><%- String(formData.rollno) %></strong>)
        <strong
          ><%- String(formData.relation) %> <%- String(formData.parentName)
          %></strong
        >
        is a bonafide student of this College studying in
        <strong
          ><%- String(formData.year) %> Year, <%- String(formData.course) %> -
          <%- String(formData.branch) %></strong
        >
        during the year
        <strong><%- formData.academicYear || '2025-2026' %></strong>.
      </p>

      <p class="body-text">
        This Certificate is issued to enable him/her to apply for
        <strong><%- String(formData.certificateFor) %></strong>
        <% if (formData.scholarshipType) { %> (<%-
        String(formData.scholarshipType) %>) <% } %>.
      </p>

      <div class="signature">PRINCIPAL</div>

      <div class="footer-line"></div>

      <div class="footer">
        Ph.No. 0452-2482240-42 &nbsp;&nbsp;&nbsp; Fax No: 0452-2483427
        &nbsp;&nbsp;&nbsp; Email: tceoffice@tce.edu
      </div>
    </div>
  </body>
</html>
