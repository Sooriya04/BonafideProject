<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Form Confirmation</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Comic+Neue&display=swap"
      rel="stylesheet"
    />

    <style>
      html,
      body {
        height: 100%;
        margin: 0;
      }

      body {
        background-color: #fffdf5;
        font-family: 'Arial', cursive;
        display: flex;
        flex-direction: column;
      }

      header {
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 10px 30px;
        background-color: #f1f1f1;
      }

      header img {
        width: 300px;
        max-width: 100%;
        height: auto;
      }

      @media screen and (max-width: 768px) {
        header img {
          width: 200px;
        }
      }

      @media screen and (max-width: 480px) {
        header img {
          width: 160px;
        }
      }

      /* Center content container */
      .content-wrapper {
        flex: 1;
        display: flex;
        justify-content: center;
        align-items: center;
      }

      /* Card styling */
      .form-card {
        background: white;
        border-radius: 20px;
        max-width: 420px;
        width: 100%;
        padding: 2.5rem 2rem;
        text-align: center;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
      }

      .title {
        color: #800000;
        font-size: 1.8rem;
        font-weight: bold;
        margin-bottom: 0.5rem;
      }

      .subtitle {
        color: #a05252;
        font-size: 1rem;
        margin-bottom: 1.2rem;
      }

      .btn-action {
        background-color: #800000;
        color: white;
        border: none;
        border-radius: 20px;
        padding: 10px;
        font-size: 1.1rem;
        width: 100%;
        margin-top: 10px;
      }

      .btn-action:hover {
        background-color: #660000;
        color: white;
      }
    </style>
  </head>
  <body>
    <header>
      <img
        src="https://www.tce.edu/themes/gavias_edmix/logo.svg"
        alt="TCE Logo"
      />
    </header>

    <div class="content-wrapper">
      <div class="card form-card">
        <h2 class="title">Form Confirmed</h2>
        <p><%= name %> visit office room for more details</p>
        <a href="/" class="btn btn-action">Go to Home</a>
      </div>
    </div>
  </body>
</html>
